# List of items
L = ['Network', 'Bio', 'Programming', 'Physics', 'Music']

# Iterate through the list using a loop
for i in range(len(L)):
    # Check if the item starts with 'B'
    if L[i].startswith('B'):
        # Print the item
        print(L[i])